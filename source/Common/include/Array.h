#pragma once

namespace Core
{
	/*template<typename _Value, std::size_t _Max>
	class Array
	{
		struct Handle
		{
			std::uint16_t id;
			std::uint16_t gen;

			bool operator==(const Handle& other)
			{
				return id == other.id && gen == other.gen;
			}
		};
	};*/
}